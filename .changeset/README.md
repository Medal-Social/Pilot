# Changesets

This folder stores release notes for publishable changes.

Create a changeset for any user-facing or package release change:

```bash
pnpm changeset
```

Use the resulting markdown file as the source of truth for the release PR and published changelog.
